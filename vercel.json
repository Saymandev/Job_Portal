{
  "buildCommand": "npm install && npm run build --filter=frontend",
  "devCommand": "npm run dev --filter=frontend",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/frontend/.next",
  "rootDirectory": "apps/frontend"
}
